<ol class="h-feed">
  <% @posts.each do |post| %>
    <%= render post.post_type %>
  <% end %>

  <%#= render partial: "/posts/post", collection: @posts, as: :post %>
</ol>

<%= will_paginate @posts unless @posts.is_a?(Array) %>
